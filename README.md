# API Usuarios (FastAPI + PostgreSQL)

Esta API permite exponer los datos de la tabla `usuarios` desde una base de datos PostgreSQL.

## Comandos para ejecutar en local

```bash
uvicorn app.main:app --reload --port 8000
